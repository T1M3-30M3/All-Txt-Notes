Convert Pcapng to Pcap:

tshark -F pcap -r {pcapng file} -w {pcap file}

find . -type f -name '*.pcapng' -print0 | while IFS= read -r -d '' f; do
    editcap -F pcap "$f" "${f%.pcapng}.pcap"
done

find . -type f -name '*.pcapng' -print0 | while IFS= read -r -d '' f; do editcap -F libpcap "$f" "${f%.pcapng}.pcap"; done

editcap -F pcap input_file.pcapng output_file.pcap
editcap input.pcapng output.pcap

for file in *.pcapng; do
  editcap -F pcap "$file" "${file%.pcapng}.pcap"
done

for file in *.pcapng; do editcap -F pcap "$file" "${file%.pcapng}.pcap"; done

tcpdump -r capture.pcap

