strings file.pcap | grep -E 'User-Agent:' | sort | uniq
Extracts and lists unique User-Agent headers from the pcap file.

strings file.pcap | grep -E 'Linux'
Searches for mentions of "Linux" in the pcap file.

strings file.pcap | grep -E 'product|launch'
Looks for occurrences of "product" or "launch".

strings file.pcap | grep -E 'Host:'
Finds all "Host" headers in the pcap file.

strings file.pcap | grep -E 'HTTP/[0-9]\.[0-9] (GET|POST|PUT|DELETE|PATCH|HEAD|OPTIONS)'
Extracts HTTP request lines that include the specified HTTP methods.

strings file.pcap | grep -Eo '([0-9]{1,3}\.){3}[0-9]{1,3}'
Extracts and lists IP addresses from the pcap file.

strings file.pcap | grep -i 'User-Agent:' | sort | uniq
Case-insensitive search for unique User-Agent headers.

strings file.pcap | grep -E 'HTTP/[0-9]\.[0-9] [0-9]{3}' | grep -E '200|404|301'
Extracts HTTP response codes 200, 404, or 301.

strings file.pcap | grep -E 'A\?'
Searches for "A?" strings, which might indicate URL parameters or data.

strings file.pcap | grep -i 'Server Name Indication' | sort | uniq
Searches for Server Name Indication (SNI) fields in the pcap file.

strings file.pcap | grep -i 'Referer:' | sort | uniq
Extracts unique Referer headers.

strings file.pcap | grep -i 'Cookie:' | sort | uniq
Lists unique Cookie headers.

strings file.pcap | grep -i 'Content-Type:' | sort | uniq
Extracts unique Content-Type headers.

strings file.pcap | grep -i 'Content-Length:' | sort | uniq
Extracts unique Content-Length headers.

strings file.pcap | grep -i 'Authorization: Basic'
Searches for Basic Authorization headers.

strings file.pcap | grep -i 'eval('
Searches for potentially dangerous eval() calls.

strings file.pcap | grep -i '-----BEGIN CERTIFICATE-----'
Looks for SSL certificates in the pcap file.

strings file.pcap | grep -Eo 'http[s]?://[^ ]+'
Extracts all HTTP/HTTPS URLs from the pcap.

strings file.pcap | grep -Eo '[A-Za-z0-9+/=]{20,}'
Extracts base64-like strings (may indicate tokens, credentials, etc.).

strings file.pcap | grep -i 'X-Forwarded-For'
Extracts the X-Forwarded-For header (often used to track IP addresses through proxies).

strings file.pcap | grep -E 'HTTP/[0-9]\.[0-9] (GET|POST|PUT|DELETE)' | wc -l
Counts the number of HTTP requests with specific methods.

strings file.pcap | grep -Eo '[A-Za-z0-9+/=]{32,}'
Extracts longer base64-like strings, possibly for tokens or passwords.

strings file.pcap | grep -Eo 'https?://([a-zA-Z0-9.-]+)' | sort | uniq
Extracts and lists unique domain names from URLs.

strings file.pcap | grep -E 'HTTP/[0-9]\.[0-9] (TRACE|CONNECT)' | sort | uniq
Searches for TRACE or CONNECT HTTP methods (may indicate potential security risks).

strings file.pcap | grep -E 'HTTP/[0-9]\.[0-9] [5][0-9]{2}' | sort | uniq
Extracts HTTP response codes 5xx (server errors).

strings file.pcap | grep -i 'User-Agent:.*(MSIE|Safari)' | sort | uniq
Finds User-Agent strings that contain MSIE (Internet Explorer) or Safari.

strings file.pcap | grep -E 'GET|POST|PUT|DELETE|HEAD' | grep -i 'Host:' | sort | uniq
Extracts HTTP requests with specific methods that include a "Host" header.

strings file.pcap | grep -Eo 'http[s]?://[^\s?]+[?][^\s]+' | sort | uniq
Extracts URLs that contain query parameters.

strings file.pcap | grep -i 'Cookie:.*session' | sort | uniq
Extracts session-related cookies.

strings file.pcap | grep -Eo '([0-9]{1,3}\.){3}[0-9]{1,3}' | sort | uniq
Extracts and lists unique IP addresses.

strings file.pcap | grep -E 'HTTP/[0-9]\.[0-9] (GET|POST|PUT|DELETE|PATCH|OPTIONS) 200' | sort | uniq
Extracts successful HTTP requests (status code 200).

strings file.pcap | grep -Eo '/[a-zA-Z0-9/_-]+\.(jpg|css|js|png|gif|html)' | sort | uniq
Extracts paths to common file types like images, CSS, JavaScript, etc.

strings file.pcap | grep -i 'Authorization: Bearer [A-Za-z0-9_-]{50,}' | sort | uniq
Extracts Bearer tokens (used for authorization).

strings file.pcap | grep -i 'Client Hello' | sort | uniq
Finds "Client Hello" messages (part of the SSL/TLS handshake).

strings file.pcap | grep -i 'A\?'
Looks for occurrences of "A?" in the file.

strings file.pcap | grep -i 'X-XSS-Protection:' | sort | uniq
Extracts unique values for the X-XSS-Protection header (used for cross-site scripting protection).

strings file.pcap | grep -i 'api_key' | sort | uniq
Searches for API keys in the pcap file.

strings file.pcap | grep -i 'Referer:.*http' | sort | uniq
Extracts Referer headers that contain URLs.

strings file.pcap | grep -i 'eval(' | sort | uniq
Looks for potential JavaScript code injection via eval().

strings file.pcap | grep -i '-----BEGIN CERTIFICATE-----' -A 20 | sort | uniq
Extracts SSL certificate information along with 20 subsequent lines.

strings file.pcap | grep -E 'HTTP/[0-9]\.[0-9] (PUT|DELETE)' | wc -l
Counts the number of HTTP PUT or DELETE requests.

strings file.pcap | grep -i 'Host:' | sort | uniq
Lists unique Host headers.

strings file.pcap | grep -E 'HTTP/[0-9]\.[0-9] (GET|POST|PUT|DELETE|PATCH|OPTIONS) [^\s]+[?].*' | sort | uniq
Extracts HTTP requests with query parameters.

strings file.pcap | grep -i '-----BEGIN CERTIFICATE-----' -A 20
Extracts SSL certificates, displaying 20 lines after the certificate header.

strings file.pcap | grep -i 'OR 1=1' | sort | uniq
Searches for SQL injection patterns like OR 1=1.

strings file.pcap | grep -i '<script>' | sort | uniq
Looks for potential cross-site scripting (XSS) attacks (<script> tags).

strings file.pcap | grep -Eo '/[a-zA-Z0-9/_-]+\.(jpg|css|js|png|gif|html|php|xml|json)' | sort | uniq
Extracts paths to files of various types (images, CSS, JS, HTML, etc.).

strings file.pcap | grep -E 'HTTP/[0-9]\.[0-9] [3][0-9]{2}' | sort | uniq
Extracts HTTP response codes 3xx (redirection).

strings file.pcap | grep -i 'Authorization: Basic' | sort | uniq
Extracts Basic Authorization headers.

strings file.pcap | grep -i 'Authorization: Bearer' | sort | uniq
Extracts Bearer Authorization headers.

strings file.pcap | grep -Eo 'http[s]?://[^\s]+' | sort | uniq
Extracts and lists all unique URLs.

strings file.pcap | grep -i 'A\?' | sort | uniq
Extracts unique occurrences of "A?".

strings file.pcap | grep -Eo '[A-Za-z0-9+/=]{20,}' | sort | uniq
Extracts long base64-like strings.

strings file.pcap | grep -i 'User-Agent:' | sort | uniq
Extracts unique User-Agent headers.

strings file.pcap | grep -E 'HTTP/[0-9]\.[0-9] [5-4][0-9]{2}' | sort | uniq
Extracts 4xx and 5xx HTTP response codes.

strings file.pcap | grep -i 'eval('
Searches for eval() JavaScript functions.

strings file.pcap | grep -i 'Host:' | sed 's/Host: //g' | sort | uniq
Extracts unique Host headers, removing the "Host: " part.

strings file.pcap | grep -i 'Client Hello' | sort | uniq
Finds SSL/TLS Client Hello messages.

strings file.pcap | grep -Eo '([0-9]{1,3}\.){3}[0-9]{1,3}' | sort | uniq
Extracts unique IP addresses.

strings file.pcap | grep -Eo 'HTTP/[0-9]\.[0-9] (GET|POST|PUT|DELETE|PATCH|HEAD|OPTIONS|TRACE|CONNECT)' | sort | uniq
Lists all HTTP methods (GET, POST, etc.) including TRACE and CONNECT